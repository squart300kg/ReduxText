import {
    SafeAreaView,
    StyleSheet,
    ScrollView,
    View,
    Text,
    Button,
    StatusBar,
    Alert,
    FlatList,
    TouchableOpacity,
  } from 'react-native';
  
const hello = () => {
    return (
        <View>
            <Text>안녕</Text>
        </View>
    );
}

export default hello;